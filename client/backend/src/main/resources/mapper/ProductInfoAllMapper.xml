<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuafu.web.mapper.ProductInfoAllMapper">
    <sql id="product_info_list_sql">
        select
        pi.coupon_amount   ,
        pi.mini_program_path   ,
        pi.product_info_id   ,
        pi.price   ,
        pi.product_name   


        FROM product_info pi

        </sql>

        <resultMap id="selectListNewMap" type="com.kuafu.web.entity.ProductInfoAll">

        <result property="couponAmount" column="coupon_amount"/>

        <result property="miniProgramPath" column="mini_program_path"/>

        <result property="productInfoId" column="product_info_id"/>

        <result property="price" column="price"/>

        <result property="productName" column="product_name"/>



                <collection property="image" ofType="com.kuafu.common.entity.StaticResource">
                            <id property="resourceId" column="image_resource_id"/>
                            <result property="resourceName" column="image_resource_name"/>
                            <result property="resourcePath" column="image_resource_path"/>
                            <result property="relatedTableName" column="image_related_table_name"/>
                            <result property="relatedTableKey" column="image_related_table_key"/>
                            <result property="relateTableColumnName" column="image_relate_table_column_name"/>
                </collection>

        </resultMap>

<select id="selectListNew"  resultMap="selectListNewMap">
select
pi.coupon_amount   ,
pi.mini_program_path   ,
pi.product_info_id   ,
pi.price   ,
pi.product_name   

        ,image.resource_id as  image_resource_id
        ,image.resource_name as image_resource_name
        ,image.resource_path as  image_resource_path
        ,image.related_table_name as  image_related_table_name
        ,image.related_table_key as  image_related_table_key
        ,image.relate_table_column_name as image_relate_table_column_name


        FROM product_info pi


        left join
        (select resource_id,
        resource_name,
        resource_path,
        related_table_name,
        related_table_key,
        relate_table_column_name
        from static_resources where related_table_name='product_info' and relate_table_column_name='image') as image
        on image.related_table_key=pi.product_info_id

${ew.customSqlSegment}
        </select>

<select id="selectList" resultType="com.kuafu.web.entity.ProductInfoAll">
select
pi.coupon_amount   ,
pi.mini_program_path   ,
pi.product_info_id   ,
pi.price   ,
pi.product_name   


        FROM product_info pi


${ew.customSqlSegment}
        </select>

<select id="selectPage" resultType="com.kuafu.web.entity.ProductInfoAll">
select
pi.coupon_amount  ,
pi.mini_program_path  ,
pi.product_info_id  ,
pi.price  ,
pi.product_name  



        FROM product_info pi





${ew.customSqlSegment}
        </select>

<select id="selectById" resultType="com.kuafu.web.entity.ProductInfoAll">
select
pi.coupon_amount  ,
pi.mini_program_path  ,
pi.product_info_id  ,
pi.price  ,
pi.product_name  


        FROM product_info pi



        where pi.product_info_id = #{id}
        </select>

<select id="selectCount" resultType="java.lang.Long">
select
count(*)



FROM product_info pi



${ew.customSqlSegment}


        </select>

        </mapper>
